# Network module

Creates one VPC with public and private subnets, an internet gateway, and a single NAT gateway for private workload egress. The single NAT gateway keeps the example cost-conscious; production can use one per availability zone for higher availability.
