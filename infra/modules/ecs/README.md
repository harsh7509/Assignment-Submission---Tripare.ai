# ECS module

Creates an internet-facing ALB, ECS/Fargate cluster and service, task definition, CloudWatch logs, and separate ALB/ECS security groups. Tasks run in private subnets and accept application traffic only from the ALB security group.
